interface:
  display_name: "QA Delegate"
  short_description: "Verify feature correctness and produce acceptance recommendations"
  default_prompt: "Use $verifying-feature-quality to create a coverage matrix, run QA verification, issue an acceptance recommendation, and return using the software-builder subagent return contract with budget + trace metadata."
