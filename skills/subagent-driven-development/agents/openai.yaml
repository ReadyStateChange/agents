interface:
  display_name: "Subagent-Driven Dev"
  short_description: "Execute plans with implementer and reviewer agents"
  default_prompt: "Use $subagent-driven-development to execute this approved plan task-by-task with implementer, spec-review, and code-quality review loops. For JavaScript/TypeScript work, pass the approved testing guidance from $javascript-testing-expert through to Stage 10 execution. Persist budget + trace telemetry for every subagent return in Stage 10 artifacts."
