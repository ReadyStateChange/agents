interface:
  display_name: "Consistency Delegate"
  short_description: "Keep all lifecycle artifacts synchronized as truth changes"
  default_prompt: "Use $maintaining-artifact-consistency to resolve cross-artifact conflicts and apply ripple updates across the lifecycle."
