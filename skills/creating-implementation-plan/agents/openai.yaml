interface:
  display_name: "Implementation Plan Delegate"
  short_description: "Draft slice-first implementation plan tied to specs"
  default_prompt: "Use $creating-implementation-plan to draft a slice-first implementation plan mapped to functional and technical contracts. For JavaScript/TypeScript work, derive each slice's test strategy and test plan from the Stage 7 testing guidance with $javascript-testing-expert. Return using the software-builder subagent return contract with Status, Artifact updates, Budget snapshot, Trace metadata, Verification references, and Next action."
