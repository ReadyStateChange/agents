interface:
  display_name: "Shape-To-Spec Delegate"
  short_description: "Translate shaped intent into draft behavioral contracts"
  default_prompt: "Use $translating-shape-to-spec to convert selected shape and breadboard artifacts into a traceable draft functional spec. Return using the software-builder subagent return contract with Status, Artifact updates, Budget snapshot, Trace metadata, Verification references, and Next action."
